const Amount = require("../models/Amount");
exports.createAmount = async (req, res) => { try { const amount = await Amount.create(req.body); res.status(201).json(amount); } catch (error) { res.status(400).json({ error: error.message }); } };
exports.getAllAmounts = async (req, res) => { try { res.status(200).json(await Amount.findAll()); } catch (error) { res.status(500).json({ error: error.message }); } };
exports.getAmountById = async (req, res) => { try { const amount = await Amount.findByPk(req.params.id); if (!amount) return res.status(404).json({ message: "Amount not found" }); res.status(200).json(amount); } catch (error) { res.status(500).json({ error: error.message }); } };
exports.updateAmount = async (req, res) => { try { const amount = await Amount.findByPk(req.params.id); if (!amount) return res.status(404).json({ message: "Amount not found" }); await amount.update(req.body); res.status(200).json(amount); } catch (error) { res.status(400).json({ error: error.message }); } };
