const Addresses = require("../models/Addresses"); const User = require("../models/User");
exports.createAddress = async (req, res) => { const { user, addresses } = req.body; try { const newAddress = await Addresses.create({ user, addresses }); res.status(201).json({ message: "Address added successfully", newAddress }); } catch (error) { res.status(400).json({ error: error.message }); } };
exports.getAllAddresses = async (req, res) => { try { res.status(200).json(await Addresses.findAll({ include: [User] })); } catch (error) { res.status(500).json({ error: error.message }); } };
exports.getAddressesByUserId = async (req, res) => { try { const addresses = await Addresses.findAll({ where: { user: req.params.userId }, include: [User] }); if (!addresses.length) return res.status(404).json({ message: "No addresses found for this user" }); res.status(200).json(addresses); } catch (error) { res.status(500).json({ error: error.message }); } };
exports.updateAddress = async (req, res) => { try { const address = await Addresses.findByPk(req.params.addressId); if (!address) return res.status(404).json({ message: "Address not found" }); await address.update({ addresses: req.body.addresses }); res.status(200).json({ message: "Address updated successfully", updatedAddress: address }); } catch (error) { res.status(400).json({ error: error.message }); } };
exports.deleteAddress = async (req, res) => { try { const address = await Addresses.findByPk(req.params.addressId); if (!address) return res.status(404).json({ message: "Address not found" }); await address.destroy(); res.status(200).json({ message: "Address deleted successfully", deletedAddress: address }); } catch (error) { res.status(500).json({ error: error.message }); } };
