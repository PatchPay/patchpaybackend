const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const Notification = sequelize.define("Notification", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, recipientId: { type: DataTypes.INTEGER, allowNull: false, field: "recipient_id", references: { model: "users", key: "id" } }, senderId: { type: DataTypes.INTEGER, field: "sender_id", references: { model: "users", key: "id" } }, title: { type: DataTypes.STRING, allowNull: false }, message: { type: DataTypes.STRING, allowNull: false }, type: { type: DataTypes.ENUM("success", "error", "info", "warning"), defaultValue: "info" }, isRead: { type: DataTypes.BOOLEAN, defaultValue: false, field: "is_read" }, category: { type: DataTypes.ENUM("transfer", "wallet", "account", "system"), allowNull: false }, metadata: { type: DataTypes.JSONB, defaultValue: {} } }, { tableName: "notifications", underscored: true, timestamps: true, updatedAt: false, indexes: [{ fields: ["recipient_id", "created_at"] }, { fields: ["recipient_id", "is_read"] }] }); Notification.associate = (models) => { Notification.belongsTo(models.User, { as: "recipient", foreignKey: "recipientId" }); Notification.belongsTo(models.User, { as: "sender", foreignKey: "senderId" }); }; module.exports = Notification;
