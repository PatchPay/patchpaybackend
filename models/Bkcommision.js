const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const BkCommissions = sequelize.define("BkCommissions", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, uprn: { type: DataTypes.STRING, allowNull: false }, amount: { type: DataTypes.INTEGER, allowNull: false, references: { model: "amounts", key: "id" } }, currency: { type: DataTypes.STRING, allowNull: false } }, { tableName: "bk_commissions", underscored: true, timestamps: true }); BkCommissions.associate = (models) => { BkCommissions.belongsTo(models.Amount, { foreignKey: "amount" }); }; module.exports = BkCommissions;
