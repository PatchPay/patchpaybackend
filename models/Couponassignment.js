const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const CouponAssignment = sequelize.define("CouponAssignment", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, user: { type: DataTypes.INTEGER, allowNull: false, references: { model: "users", key: "id" } }, coupon: { type: DataTypes.INTEGER, allowNull: false, references: { model: "coupons", key: "id" } } }, { tableName: "coupon_assignments", underscored: true, timestamps: true }); CouponAssignment.associate = (models) => { CouponAssignment.belongsTo(models.User, { foreignKey: "user" }); CouponAssignment.belongsTo(models.Coupon, { foreignKey: "coupon" }); }; module.exports = CouponAssignment;
