const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const Request = sequelize.define("Request", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, request_number: { type: DataTypes.STRING, allowNull: false, unique: true }, reason: { type: DataTypes.STRING, allowNull: false }, additional_info: { type: DataTypes.STRING, defaultValue: "" }, url_photo: { type: DataTypes.JSONB, defaultValue: [] }, uprn: { type: DataTypes.STRING, allowNull: false }, quote_number: { type: DataTypes.INTEGER, allowNull: false, references: { model: "quotes", key: "id" } } }, { tableName: "refund_requests", underscored: true, timestamps: true }); Request.associate = (models) => { Request.belongsTo(models.Quote, { foreignKey: "quote_number" }); }; module.exports = Request;
