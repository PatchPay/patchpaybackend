const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const Balance = sequelize.define("Balance", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, balance: { type: DataTypes.INTEGER, allowNull: false, references: { model: "amounts", key: "id" } }, available_balance: { type: DataTypes.INTEGER, allowNull: false, references: { model: "amounts", key: "id" } }, currency: { type: DataTypes.STRING, allowNull: false }, user: { type: DataTypes.INTEGER, allowNull: false, references: { model: "users", key: "id" } } }, { tableName: "balances", underscored: true, timestamps: true }); Balance.associate = (models) => { Balance.belongsTo(models.Amount, { as: "balanceAmount", foreignKey: "balance" }); Balance.belongsTo(models.Amount, { as: "availableBalanceAmount", foreignKey: "available_balance" }); Balance.belongsTo(models.User, { foreignKey: "user" }); }; module.exports = Balance;
