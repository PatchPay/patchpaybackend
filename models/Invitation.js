const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const Invitation = sequelize.define("Invitation", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, contact: { type: DataTypes.STRING, allowNull: false, validate: { notEmpty: true } }, type: { type: DataTypes.ENUM("email"), defaultValue: "email" }, token: { type: DataTypes.STRING, allowNull: false, unique: true }, expiresAt: { type: DataTypes.DATE, allowNull: false, field: "expires_at" } }, { tableName: "invitations", underscored: true, timestamps: true, updatedAt: false, indexes: [{ fields: ["contact"] }, { fields: ["expires_at"] }] }); Invitation.associate = () => {}; module.exports = Invitation;
