const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const Payment = sequelize.define("Payment", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, user: { type: DataTypes.INTEGER, allowNull: false, references: { model: "users", key: "id" } }, amount: { type: DataTypes.FLOAT, allowNull: false, validate: { min: 0 } }, currency: { type: DataTypes.STRING, allowNull: false, defaultValue: "NGN" }, payment_method: { type: DataTypes.ENUM("Bank Transfer", "Card", "Wallet"), allowNull: false }, payment_type: { type: DataTypes.ENUM("Escrow Funding", "Escrow Release", "Escrow Refund", "Wallet Funding"), allowNull: false }, transaction_reference: { type: DataTypes.STRING, allowNull: false, unique: true }, status: { type: DataTypes.ENUM("Pending", "Completed", "Failed", "Refunded"), defaultValue: "Pending" }, metadata: { type: DataTypes.JSONB, defaultValue: {} } }, { tableName: "payments", underscored: true, timestamps: true, indexes: [{ fields: ["user", "created_at"] }, { fields: ["status"] }] }); Payment.associate = (models) => { Payment.belongsTo(models.User, { foreignKey: "user" }); Payment.hasMany(models.Refund, { foreignKey: "payment" }); }; module.exports = Payment;
