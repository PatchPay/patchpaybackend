const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const Refund = sequelize.define("Refund", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, payment: { type: DataTypes.INTEGER, allowNull: false, references: { model: "payments", key: "id" } }, amount: { type: DataTypes.INTEGER, allowNull: false, references: { model: "amounts", key: "id" } }, reason: { type: DataTypes.STRING, allowNull: false }, status: { type: DataTypes.ENUM("Requested", "Processed", "Rejected"), defaultValue: "Requested" } }, { tableName: "refunds", underscored: true, timestamps: true }); Refund.associate = (models) => { Refund.belongsTo(models.Payment, { foreignKey: "payment" }); Refund.belongsTo(models.Amount, { foreignKey: "amount" }); }; module.exports = Refund;
