const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const makeReference = () => `ESC-TXN-${Date.now()}-${Math.floor(Math.random() * 10000).toString().padStart(4, "0")}`;
const EscrowTransaction = sequelize.define("EscrowTransaction", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, transactionReference: { type: DataTypes.STRING, allowNull: false, unique: true, field: "transaction_reference" }, escrowId: { type: DataTypes.INTEGER, allowNull: false, field: "escrow_id", references: { model: "escrows", key: "id" } }, userId: { type: DataTypes.INTEGER, allowNull: false, field: "user_id", references: { model: "users", key: "id" } }, type: { type: DataTypes.ENUM("FUND", "RELEASE", "REFUND"), allowNull: false }, amount: { type: DataTypes.FLOAT, allowNull: false }, currency: { type: DataTypes.STRING, allowNull: false }, balanceAfterTransaction: { type: DataTypes.FLOAT, allowNull: false, field: "balance_after_transaction" }, outstandingBalanceAfterTransaction: { type: DataTypes.FLOAT, allowNull: false, field: "outstanding_balance_after_transaction" }, originalAmount: { type: DataTypes.FLOAT, allowNull: false, field: "original_amount" }, status: { type: DataTypes.ENUM("PENDING", "COMPLETED", "FAILED"), defaultValue: "PENDING" }, metadata: { type: DataTypes.JSONB } }, { tableName: "escrow_transactions", underscored: true, timestamps: true, hooks: { async beforeValidate(transaction) { if (!transaction.transactionReference) { do { transaction.transactionReference = makeReference(); } while (await EscrowTransaction.findOne({ where: { transactionReference: transaction.transactionReference } })); } } } }); EscrowTransaction.associate = (models) => { EscrowTransaction.belongsTo(models.Escrow, { foreignKey: "escrowId" }); EscrowTransaction.belongsTo(models.User, { foreignKey: "userId" }); }; module.exports = EscrowTransaction;
