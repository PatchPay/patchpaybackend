const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const MinAmount = sequelize.define("MinAmount", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, squad: { type: DataTypes.FLOAT, allowNull: false }, stripe: { type: DataTypes.FLOAT, allowNull: false } }, { tableName: "min_amounts", underscored: true, timestamps: true }); MinAmount.associate = () => {}; module.exports = MinAmount;
