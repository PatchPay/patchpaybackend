const { DataTypes } = require("sequelize"); const sequelize = require("../config/database");
const CreditMyAccountBks = sequelize.define("CreditMyAccountBks", { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true }, amount: { type: DataTypes.INTEGER, allowNull: false, references: { model: "amounts", key: "id" } }, uprn: { type: DataTypes.STRING, allowNull: false }, user_ref: { type: DataTypes.INTEGER, allowNull: false, references: { model: "users", key: "id" } }, status: { type: DataTypes.ENUM("Pending", "Accepted", "Rejected"), allowNull: false } }, { tableName: "credit_my_account_bks", underscored: true, timestamps: true }); CreditMyAccountBks.associate = (models) => { CreditMyAccountBks.belongsTo(models.Amount, { foreignKey: "amount" }); CreditMyAccountBks.belongsTo(models.User, { foreignKey: "user_ref" }); }; module.exports = CreditMyAccountBks;
